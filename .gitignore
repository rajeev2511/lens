# build
bin/
obj/
*.user
.vs/
TestResults/
node_modules/
.angular/
src/Lens.Api/wwwroot/

# local data and tools (downloaded by scripts/)
data/
tools/ffmpeg/
tools/*.zip
models/*.onnx
*.mp4
*.ts
*.mkv
# '*.ts' above is meant for MPEG transport streams; it also matches TypeScript,
# which silently kept the Angular front end out of the repo. Keep the sources.
!src/Lens.Web/**/*.ts
!samples/README.md

# secrets
.env
appsettings.*.local.json
